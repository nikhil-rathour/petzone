import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "images.unsplash.com",
      "images.pexels.com",
      "media.licdn.com",
      "media.licdn.com",
      "www.pedigree.com",
      "www.petsy.online",
      "petsy.online",
    ],
  },
};

export default nextConfig;
